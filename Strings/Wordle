t = int(input())

for _ in range(t):
    S = input().strip()
    T = input().strip()
    
    M = ""
    
    for i in range(5):
        if S[i] == T[i]:
            M += 'G'
        else:
            M += 'B'
    
    print(M)
