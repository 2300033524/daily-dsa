t = int(input())

for _ in range(t):
    n = int(input())
    arr = list(map(int, input().split()))

    arr.sort()

    minimum = float('inf')

    for i in range(n - 1):
        diff = arr[i + 1] - arr[i]

        if diff < minimum:
            minimum = diff

    print(minimum)
